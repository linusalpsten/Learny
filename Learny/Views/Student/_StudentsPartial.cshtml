@model IEnumerable<Learny.Models.ApplicationUser>

@*<ul>
    @foreach (var item in Model)
    {
        <li>
            @Html.ActionLink(item.Name, "Details", new { email = item.Email })
        </li>
    }
</ul>*@


<table class="table">
    <tr class="headerRow">
        <th>Elev</th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td> @Html.ActionLink(item.Name, "Details", new { email = item.Email })                     </td>
        </tr>
    }

</table>
