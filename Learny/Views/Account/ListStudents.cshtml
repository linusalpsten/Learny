@model IEnumerable<Learny.ViewModels.StudentViewModel>

@{
    ViewBag.Title = "Studenter";
}

<h2>@ViewBag.Title</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AttendingCourse)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {

        <tr>
            <td>
                @Html.ActionLink(item.Name, "StudentDetails", new { email = item.Email })
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AttendingCourse)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
        </tr>
    }

</table>
